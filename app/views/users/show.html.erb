<div class="container">
	<% cache @user do %>
	<h1> <%= @user.name %>, <%= @user.email %> </h1>

	<% provide(:title, @user.name) %>
	<h1>
	  <%= gravatar_for @user %>
	  <%= @user.name %>
	</h1>
	<% end %>
	</div>
</div>